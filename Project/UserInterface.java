package Project;

import javax.swing.JFrame;

// After the login it should initialize the Weighted Graph

public class UserInterface {
    public void initialize() {
        // Create the JFrame and set up the GUI components

        JFrame frame = new JFrame("Bus Station Management System");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(800, 600);
        frame.setVisible(true);

        // Display the warning with and ok button to move on to the login page.

        frame.}
}
